class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        def concat(nums):
            for i in range(0,len(nums)):
                ans.append(nums[i])
        ans=[]
        concat(nums)
        concat(nums)
        return ans